language: python
python:
    - "3.4"
    - "3.6"
    - "nightly"
install: 
    - pip install -r tests/requirements.txt
    - python -m spacy.en.download all
script: python -m pytest --spec --flake --cov=eulerbot --cov-report term-missing tests/
